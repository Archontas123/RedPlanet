export const CHUNK_SIZE = 1000; // Size of each chunk in world units (pixels)
export const LOAD_RADIUS = 2; // Load chunks within this radius (in chunks) around the player's chunk
export const TILE_SIZE = 40; // Size of each tile in world units (pixels) for caves, etc.
